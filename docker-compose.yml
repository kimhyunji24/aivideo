services:
  redis:
    image: redis:7-alpine
    container_name: aivideo-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - aivideo_redis_data:/data
    restart: unless-stopped

volumes:
  aivideo_redis_data:
